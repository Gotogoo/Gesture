pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                sh 'echo build'
            }
        }

        stage('deploy-qa') {
            steps {
                sh 'echo deploy-qa'
            }
        }

        stage('deploy-st') {
                sh 'echo deploy-st'
            steps {
            }
        }

        stage('deploy-uat') {
            steps {
                sh 'echo deploy-uat'
            }
        }
    }
}