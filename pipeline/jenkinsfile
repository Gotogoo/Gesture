pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                sh 'echo build'
            }
        }

        stage('deploy-qa') {
            steps {
                sh 'echo deploy-qa'
            }
        }

        stage('deploy-st') {
            steps {
                sh 'echo deploy-st'
            }
        }

        stage('deploy-uat') {
            steps {
                sh 'echo deploy-uat'
            }
        }
    }
}